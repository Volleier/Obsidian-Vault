# 介绍
点裁剪作为其他复杂裁剪（如线段、多边形裁剪）的基础，能快速排除明显不可见的元素，提高图形管线效率。其核心是判断一个给定的点 $P(x,y)$ 是否位于裁剪窗口内部。
点裁剪作为图形裁剪的基础内容，实现并不复杂

裁剪窗口是一个轴对齐矩形，由两个点定义：左下角 $(x_{min}, y_{min})$ 和右上角 $(x_{max}, y_{max})$。

对于任意一点 $P(x, y)$，其裁剪规则并不复杂。点 P 被保留（可见）的充分必要条件是：

$$x_{min}≤x≤x_{max}且y_{min}≤y≤y_{max}$$
